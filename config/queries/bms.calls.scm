; BMS doesn't have calls - it's a declarative screen definition language
; This file exists for consistency with other languages

; Cross-reference: map sets referenced by name
(bms_statement
  (label) @reference.mapset)
