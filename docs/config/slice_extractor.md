# Slice Extractor Config

Local config for the slice extractor stage.

## Parameters
- `run_id`, `artifact_version`, `output_root`: use global defaults if not set.
- `slice_max_nodes`: cap slice size (if supported).
- `slice_depth_limit`: traversal depth (if supported).
